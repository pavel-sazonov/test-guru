<div class="row justify-content-center mt-3">
  <div class="col col-md-4">

    <h1>
      <small class="text-muted"><%= t('.header') %></small>
    </h1>

    <table class="table table-striped">
      <thead>
        <tr>
          <td scope="col"><%= t('.question') %></th>
          <td scope="col"><%= t('.url') %></th>
          <td scope="col"><%= t('.email') %></th>
        </tr>
      </thead>

      <tbody>
        <%= render @gists %>
      </tbody>

    </table>
  </div>
</div>
